import {recipe} from "@vanilla-extract/recipes";


const errorStyle = recipe({
    base: {
        borderRadius: 20,
        color: "red",
        fontWeight: "bold"

    }
})
export default errorStyle